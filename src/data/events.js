/**
 * Data table of all race events: id, weight, time window, effect, animation, commentary,
 * optional drinking rule. Pure data — the logic lives in engine/effects.js.
 *
 * Implementation follows in M2 (see docs/05_MILESTONES.md).
 */

export {};
