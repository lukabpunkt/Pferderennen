/**
 * Pure functions translating event definitions into speed modifiers.
 * Handles attack/release fades, sequences (such as the nap) and sticky effects.
 *
 * Implementation follows in M2 (see docs/05_MILESTONES.md).
 */

export {};
