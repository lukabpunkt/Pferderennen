/**
 * The race simulation: createRace({seed, config}) returns an instance with a fixed timestep.
 * Tracks positions, velocities, active effects, lead changes and the finish order.
 * Knows runners only as indices 0..5 — never horse names, colours or bets.
 *
 * Implementation follows in M2 (see docs/05_MILESTONES.md).
 */

export {};
