/**
 * Speed model of a single runner: phase profile, Ornstein-Uhlenbeck noise, sprints, finishing
 * kick and event effects are summed into one relative speed modifier.
 * Parameterised identically for every runner — this symmetry is what makes the race fair.
 *
 * Implementation follows in M2 (see docs/05_MILESTONES.md).
 */

export {};
