/**
 * Plans before the race which event hits which runner when, and fires it at runtime.
 * Enforces the fairness rules: uniform runner choice, 8-95 % window, at most 2 events per
 * runner, 2 s minimum gap. Exposes upcoming events so rendering can start props early.
 *
 * Implementation follows in M2 (see docs/05_MILESTONES.md).
 */

export {};
