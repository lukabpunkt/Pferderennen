/**
 * Versioned load and save of the game state in localStorage (debounced, fault tolerant).
 * Falls back silently to running without persistence in private browsing mode.
 *
 * Implementation follows in M1 (see docs/05_MILESTONES.md).
 */

export {};
