/**
 * HUD as a DOM overlay on top of the canvas: leaderboard, progress bar, commentary line,
 * event toasts. Deliberately DOM rather than canvas for crisp text and accessibility.
 *
 * Implementation follows in M3 (see docs/05_MILESTONES.md).
 */

export {};
