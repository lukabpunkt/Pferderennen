/**
 * Particle system with a fixed object pool (max 400): dust, confetti, stars, sparkles,
 * rainbow, splashes, zzz, hearts, question marks, speed lines. No allocation during a race.
 *
 * Implementation follows in M3 (see docs/05_MILESTONES.md).
 */

export {};
