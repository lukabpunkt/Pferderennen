/**
 * requestAnimationFrame loop with an accumulator: fixed simulation timestep, interpolated
 * rendering. Pauses when the tab is hidden and exposes a timeScale for the photo finish.
 *
 * Implementation follows in M2 (see docs/05_MILESTONES.md).
 */

export {};
