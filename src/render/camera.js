/**
 * Race camera: follows the field with a lerp, zooms out when the field spreads,
 * zooms onto the finish line during a photo finish and models shake trauma.
 *
 * Implementation follows in M3 (see docs/05_MILESTONES.md).
 */

export {};
